# 🚀 Пошаговая инструкция деплоя бота на Render.com

## Шаг 1: Подготовка GitHub

### 1.1 Создай GitHub аккаунт (если нет)
- Зайди на https://github.com
- Нажми "Sign up"
- Зарегистрируйся

### 1.2 Создай новый репозиторий
1. На главной странице GitHub нажми зелёную кнопку **"New"** (или **"Create repository"**)
2. Заполни:
   - **Repository name**: `telegram-homework-bot`
   - **Description**: `Бот для домашних заданий`
   - ✅ Поставь галочку **"Public"**
   - ✅ Поставь галочку **"Add a README file"**
3. Нажми **"Create repository"**

### 1.3 Загрузи файлы
1. В репозитории нажми **"Add file"** → **"Upload files"**
2. Перетащи ВСЕ файлы из папки `telegram_bot_deploy`:
   - `bot.py`
   - `school_list.json`
   - `requirements.txt`
   - `README.md`
   - `.gitignore`
   - `render.yaml`
3. Внизу нажми **"Commit changes"**

---

## Шаг 2: Регистрация на Render.com

1. Зайди на https://render.com
2. Нажми **"Get Started"** или **"Sign Up"**
3. Выбери **"Sign up with GitHub"** (авторизация через GitHub)
4. Разреши Render доступ к твоим репозиториям

---

## Шаг 3: Создание сервиса

### 3.1 Создай новый Worker
1. На главной странице Render нажми **"New +"**
2. Выбери **"Background Worker"**

### 3.2 Подключи репозиторий
1. Найди в списке репозиторий `telegram-homework-bot`
2. Нажми **"Connect"**

### 3.3 Настрой параметры
Заполни форму:

**Name**: `telegram-homework-bot` (или любое название)

**Region**: Выбери ближайший регион (например, Frankfurt)

**Branch**: `main` (или `master`)

**Runtime**: **Python 3**

**Build Command**: 
```
pip install -r requirements.txt
```

**Start Command**: 
```
python bot.py
```

### 3.4 Переменные окружения (Environment Variables)
Нажми **"Add Environment Variable"** и добавь:
- **Key**: `PYTHON_VERSION`
- **Value**: `3.11.0`

### 3.5 План (Instance Type)
Выбери **"Free"** (бесплатный)

---

## Шаг 4: Запуск!

1. Нажми большую синюю кнопку **"Create Background Worker"**
2. Подожди 2-3 минуты (Render установит библиотеки и запустит бота)
3. Статус изменится на **"Live"** ✅

---

## Шаг 5: Проверка

1. Открой логи (вкладка **"Logs"** на странице сервиса)
2. Должно появиться:
```
🚀 BOT STARTED
```
3. Открой Telegram и напиши боту `/start`
4. Бот работает! 🎉

---

## 🔧 Полезные команды

### Просмотр логов
На странице сервиса → вкладка **"Logs"**

### Перезапуск бота
Нажми **"Manual Deploy"** → **"Deploy latest commit"**

### Обновление кода
1. Измени файлы в GitHub репозитории
2. Render автоматически обновит бота (Auto-Deploy)

### Остановка бота
На странице сервиса → **"Suspend"**

---

## ⚠️ Важно!

### База данных
- `bot.db` создаётся автоматически при первом запуске
- НО! При перезапуске сервиса база может сброситься
- Для постоянного хранения нужно настроить внешнюю БД (PostgreSQL)

### Обновление school_list.json
Если нужно добавить/удалить учеников:
1. Измени файл `school_list.json` в GitHub
2. Render автоматически перезапустит бота

### Изменение токена или Admin ID
Измени в файле `bot.py` на GitHub:
```python
TOKEN = "ВАШ_ТОКЕН"
ADMIN_ID = ВАШ_ID
```

---

## 🆘 Возможные проблемы

### "Build failed"
- Проверь, что файл `requirements.txt` загружен
- Проверь правильность написания зависимостей

### "Service exited with code 1"
- Открой логи и найди ошибку
- Часто это неверный токен или синтаксическая ошибка в коде

### Бот не отвечает
- Проверь, что статус **"Live"**
- Проверь логи на наличие ошибок
- Убедись, что токен правильный

---

## 🎁 Бонус: Автоматическое обновление

Render автоматически обновляет бота при изменениях в GitHub!
1. Внеси изменения в код на GitHub
2. Commit & Push
3. Render автоматически пересобрёт и перезапустит бота

---

## 📞 Поддержка

Если что-то не работает:
1. Проверь логи в Render
2. Убедись, что все файлы загружены в GitHub
3. Проверь, что токен бота правильный

Удачи! 🚀
Made by @romasent ⭐
